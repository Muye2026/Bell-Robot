# AGENTS.md

- 所有回复用中文。
- 本目录是久坐提醒外置硬件项目，当前主线默认使用 Freenove ESP32-S3 N16R8 CAM 开发板，摄像头默认按 OV5640CSP，围绕 SSD1306 OLED、蜂鸣器、按键和 PlatformIO 固件推进。
- 每次重要修改后，必须同步更新 `README.md`，记录日期、修改内容、验证结果和下一步；`README.md` 同时作为开发进度、里程碑和交接文档。
- 每次重大更改之后，都要检查并按需更新 Git 忽略规则，避免把构建缓存、编辑器配置或其他无关产物纳入版本控制。
- 不要凭空确定摄像头引脚、板卡型号或硬件连接；未现场确认的内容必须标注为待核对。
- 固件改动后优先运行 `C:\Users\23171\.platformio\penv\Scripts\pio.exe run` 验证。
- 不要保留 `.pio/` 等构建缓存作为项目交接文件。
- 文档保持可交接、简洁明确，不写无关说明。
